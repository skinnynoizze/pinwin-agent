# Polygon RPC (used by example-balances, example-allowance, example-place-bet, example-claim)
POLYGON_RPC_URL=https://poly.api.pocket.network

# Bettor wallet (address for read-only scripts; private key for placing/claiming)
BETTOR_ADDRESS=0x...
# Hex, 64 chars; with or without 0x prefix (scripts normalize automatically)
BETTOR_PRIVATE_KEY=0x...

# Place bet (example-place-bet.mjs): [amount] only → picker; or amount conditionId outcomeId minOdds → no picker
# Claim (example-claim.mjs): pass bet IDs as args, e.g. node example-claim.mjs 215843,211524
